Orthodox FM
  solid
    $ miur
  sparse
    $ miur --all

Remote/embedded FM
  core (target) <--> ui (host)
    T: $ miur --core
    H: $ miur --ui --connect host:port

Sync FM
  Many TUI fm
  # Auto-connect to single *core* if -t <path/name> (tmux-like) wasn't used
    $ miur --tui
    $ miur --tui
    ...

Tiling WM
  GUI preview, TUI fm
    $ miur --tui
    $ miur --gui --preview

# vim:ft=toml
[settings]
os=Linux
arch=x86_64
compiler=clang
compiler.version=21
compiler.libcxx=libstdc++11
build_type=Release

[conf]
# Force the standard here so it's injected into the generated toolchain
tools.build:cstd=2y
# Ensure we use Clang 21 specifically
tools.build:compiler_executables={'c': 'clang-21', 'cpp': 'clang++-21'}
